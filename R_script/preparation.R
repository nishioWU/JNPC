# 必要なパッケージのインストール
# 最初に一度だけ、「Ctrl」 + 「Enter」を繰り返し押して、実行して下さい
# または、「Source」で一度に実行でも構いません

install.packages("tidyverse")
install.packages("plotly")
install.packages("GGally")
install.packages("scales")
install.packages("knitr")
update.packages()

# requireまたはlibraryは、Rを起動するたびに必要です
